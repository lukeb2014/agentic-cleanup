#!/bin/bash

# Run the tick.sh script that iterates through the checks as a final cleanup.
# Double-check that the periodic script created by session-end is cancelled.

# Run all scripts in the cleanup.sh folder as well.